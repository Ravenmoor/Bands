<div id='login_bar' >
	<p>
		<% if user_signed_in? %>
			Welcome, 
			<%= :user %>.
			<a class="button" href="#"><span>Sign Out</span></a>
			<!-- submit button for log out -->
		<% else %>
			<%= form_for '/application/login', :method => 'get' do %>
				<p> 
					username
					<%= text_field_tag :username, params[:username] %>
					password				
					<%= text_field_tag :password, params[:password] %>
<!--	    		<%= submit_tag "Log In", :name => nil , :class => 'button' %> -->
					<!-- <%= link_to('Create Account', new_user_registration_path, :class => 'button') %> -->
					<a class="button" href="#" ><span>Sign In</span></a>
	  			<a class="button" href="/users/sign_up" ><span>Create Account</span></a>
	  		</p>
	  	<% end %>
		<% end %>
	</p>
</div>